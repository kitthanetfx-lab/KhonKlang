# Accessibility Audit: คนกลาง (Khonklang) — ทั้งโปรเจกต์ + ฟีเจอร์ "ติดต่อพนักงาน" ใหม่
**Standard:** WCAG 2.1 AA | **Date:** 2026-06-19 | **Scope:** ทั้งไซต์ (หน้าลูกค้า + หลังบ้าน) รวมฟีเจอร์แชท/โทรกับทีมงานที่สร้างใหม่

## Summary
**Issues found:** 10 | **Critical:** 2 | **Major:** 5 | **Minor:** 3

ฟีเจอร์ใหม่ (`SupportWidget.tsx`, `admin/support/page.tsx`) ถูกออกแบบให้เข้าถึงได้ตั้งแต่แรก — มี `role="dialog"`/`role="status"`/`role="alert"`, ปิดด้วย Escape, จัดการโฟกัสตอนเปิด/ปิด, ปุ่มหลักขนาด 44×44px, `focus-visible` ครบ — **ผ่านเกือบทุกข้อ** ยกเว้น 2 จุดที่สืบทอดปัญหาจาก design token เดิมของไซต์ (ดูข้อ 3, 5 ด้านล่าง) ปัญหาส่วนใหญ่ที่พบเป็น**ปัญหาเดิมของไซต์**ที่กระทบทั้งหน้าลูกค้าและหลังบ้าน ไม่ใช่ปัญหาที่เกิดจากฟีเจอร์ใหม่

## Findings

### Perceivable

| # | Issue | WCAG Criterion | Severity | Recommendation |
|---|-------|---------------|----------|----------------|
| 1 | สี `--muted` (#6b7a9c) บนพื้นขาว/เทาอ่อนให้คอนทราสต์ 4.30:1 / 4.08:1 ไม่ถึง 4.5:1 — ใช้เป็นสีข้อความรองทั่วทั้งไซต์ (คำบรรยาย, label, แคปชัน) ในเกือบทุกหน้า รวมถึง `.sw-empty`/`.sw-loading` ในวิดเจ็ตใหม่ | 1.4.3 Contrast | 🔴 Critical | ปรับ `--muted` ให้เข้มขึ้น เช่น `#5b6a8c` หรือเข้มกว่า เพื่อให้ได้ ≥4.5:1 บนทั้งสองพื้นหลัง |
| 2 | ปุ่มข้อความสีขาวบนพื้นเขียว/แดง (`--green-500` 3.18:1, `--rose-500` 3.50:1) ไม่ถึง 4.5:1 — กระทบ `.btn-green`, `.adm-btn-approve`, `.dr-cta-green`, `.btn-danger`, `.adm-btn-reject:hover` (ทั้งหน้าลูกค้าและหลังบ้าน) | 1.4.3 Contrast | 🔴 Critical | ใช้ `--green-600`/เพิ่มความเข้มของ `--rose-500` สำหรับพื้นหลังปุ่มที่มีตัวอักษร หรือสลับเป็นข้อความสีเข้มบนพื้นอ่อน |
| 3 | สี `--rose-500` ใช้เป็นสีตัวอักษรตรงบนพื้นขาว/พื้นชมพูอ่อน ได้คอนทราสต์ ~3.50:1 — กระทบ `.ss-pain-ic`, `.csr-req`, `.csr-hit-head b` และสืบทอดมาที่ `.sw-callbar.ring` (สถานะ "พนักงานกำลังโทรเข้า" ในวิดเจ็ตใหม่) | 1.4.3 Contrast | 🟡 Major | ใช้สีเข้มกว่า (เช่นเดียวกับข้อ 2) เฉพาะกรณีใช้เป็นสีตัวอักษร |
| 4 | ไม่มี landmark `<nav>`/`role="navigation"` ครอบเมนูหลักใน `Site.tsx`/`layout.tsx` ทำให้ผู้ใช้ screen reader ข้ามไปยังเมนูโดยตรงไม่ได้ | 1.3.1 Info and Relationships | 🟢 Minor | เพิ่ม `<nav aria-label="เมนูหลัก">` ครอบลิงก์นำทางหลัก |
| 5 | (จุดแข็ง) รูปภาพทั้งหมดที่ตรวจ (`PaymentMethods`, `ScamReportForm`, `marketplace`, `deal/[id]`, `profile`, ฯลฯ) มี `alt` ที่สื่อความหมายครบ และรูปตกแต่งใช้ `alt=""` อย่างถูกต้อง — **ผ่าน** | 1.1.1 Non-text Content | ✅ Pass | ไม่ต้องแก้ |

### Operable

| # | Issue | WCAG Criterion | Severity | Recommendation |
|---|-------|---------------|----------|----------------|
| 6 | ไม่มี skip link ให้กระโดดข้ามเมนู/แบนเนอร์ไปยังเนื้อหาหลักได้ ผู้ใช้คีย์บอร์ดต้อง Tab ผ่านองค์ประกอบนำทางทุกครั้งในทุกหน้า | 2.4.1 Bypass Blocks | 🟡 Major | เพิ่มลิงก์ "ข้ามไปเนื้อหาหลัก" ที่ซ่อนด้วย visually-hidden และโชว์เมื่อโฟกัส วางเป็นองค์ประกอบแรกใน `layout.tsx` |
| 7 | `DialogProvider` (`Dialog.tsx`) ไม่ทำ focus trap — กด Tab จากใน dialog หลุดไปโฟกัสองค์ประกอบด้านหลังที่ถูก backdrop บังอยู่ได้ และปุ่ม confirm/alert ไม่ได้รับโฟกัสเริ่มต้นเลย (มีแค่ `autoFocus` ใน input ของ prompt) | 2.4.3 Focus Order | 🟡 Major | ใส่ focus trap (เช่นวนโฟกัสกลับเมื่อ Tab/Shift+Tab ถึงขอบ) และตั้งโฟกัสเริ่มต้นไปที่ปุ่มยืนยัน/ยกเลิกเมื่อ dialog เปิด |
| 8 | ปุ่มไอคอนขนาดเล็กจำนวนมากทั่วไซต์เล็กกว่า 44×44px เช่น `.dash-back`/`.dr-back`/`.sub-back`/`.reg-back` (36×36), `.cart-qty button` (30×30), `.adm-drawer-close` (30×30), และปุ่มรับ/ปฏิเสธสายใหม่ `.sw-roundbtn` (36×36) | 2.5.5 Target Size | 🟢 Minor | เพิ่ม padding/`min-width`/`min-height` เป็น 44px อย่างน้อยสำหรับปุ่มที่ใช้บนมือถือบ่อย (เช่น `.sw-roundbtn` รับ/ปฏิเสธสาย) |
| 9 | Toast (`.dlg-toast`) หายอัตโนมัติใน 3.5 วินาทีโดยไม่มีปุ่มปิดเอง/หยุดเวลาเมื่อ hover/focus — ผู้ใช้ที่อ่านช้าหรือใช้ screen reader อาจพลาดข้อความ | 2.2.1 Timing Adjustable | 🟢 Minor | เพิ่มปุ่มปิด toast เอง หรือหยุด timer ขณะ hover/focus |

### Understandable

ไม่พบปัญหาเพิ่มเติมนอกจากที่ครอบในหมวด Operable ข้อ 9 ฟอร์มหลัก (login, ฟิลด์อินพุตในวิดเจ็ตใหม่) มี label/`aria-label` ครบทุกฟิลด์ที่ตรวจสอบ

### Robust

| # | Issue | WCAG Criterion | Severity | Recommendation |
|---|-------|---------------|----------|----------------|
| 10 | `dlg-card` มี `role="dialog"` แต่ไม่มี `aria-labelledby`/`aria-describedby` เชื่อมกับ `.dlg-title`/`.dlg-msg` ทำให้ screen reader ไม่ประกาศหัวข้อ/ข้อความอัตโนมัติเมื่อโฟกัสเข้า dialog | 4.1.2 Name, Role, Value | 🟡 Major | เพิ่ม `id` ให้ title/message แล้วอ้างด้วย `aria-labelledby`/`aria-describedby` บน `.dlg-card` |

## Color Contrast Check

| Element | Foreground | Background | Ratio | Required | Pass? |
|---------|-----------|------------|-------|----------|-------|
| ข้อความหลัก (`ink`) | #0d1b3e | #ffffff | 16.89:1 | 4.5:1 | ✅ |
| ข้อความรอง (`ink-2`) | #344467 | #ffffff | 9.68:1 | 4.5:1 | ✅ |
| ข้อความรอง (`muted`) | #6b7a9c | #ffffff | 4.30:1 | 4.5:1 | ❌ |
| ข้อความรอง (`muted`) | #6b7a9c | #f7f9fd | 4.08:1 | 4.5:1 | ❌ |
| ปุ่มเขียว ตัวอักษรขาว | #ffffff | green-500 #10a566 | 3.18:1 | 4.5:1 | ❌ |
| ปุ่มเขียว ตัวอักษรขาว (เข้มขึ้น) | #ffffff | green-600 #0a8654 | 4.61:1 | 4.5:1 | ✅ |
| ปุ่มแดง ตัวอักษรขาว | #ffffff | rose-500 #ef4f6b | 3.50:1 | 4.5:1 | ❌ |
| ปุ่มน้ำเงินหลัก | #ffffff | blue-600 #1f54d6 | 6.37:1 | 4.5:1 | ✅ |
| ปุ่ม/ลิงก์ accent | #ffffff | blue-500 #2f6bf0 | 4.69:1 | 4.5:1 | ✅ |
| ข้อความสีแดงตรง | rose-500 #ef4f6b | #ffffff | 3.50:1 | 4.5:1 | ❌ |
| ข้อความสีเขียวเข้ม | green-700 #0a6b46 | green-50 #e9faf2 | 6.06:1 | 4.5:1 | ✅ |
| ราคา/ตัวเลขสีเขียว | green-600 #0a8654 | #ffffff | 4.61:1 | 4.5:1 | ✅ |
| ลิงก์/accent บนข้อความ | blue-500 #2f6bf0 | #ffffff | 4.69:1 | 4.5:1 | ✅ |

## Keyboard Navigation

| Element | Tab Order | Enter/Space | Escape | หมายเหตุ |
|---------|-----------|-------------|--------|------|
| ปุ่มลอย `.sw-fab` (วิดเจ็ตใหม่) | ตามลำดับ DOM ปกติ | เปิด/ปิดแผงแชท | ปิดแผงเมื่อเปิดอยู่ | ✅ โฟกัสย้ายไปปุ่มปิดอัตโนมัติเมื่อเปิด, กลับมาที่ FAB เมื่อปิด |
| แชทอินพุต/ปุ่มส่ง (วิดเจ็ตใหม่) | ปกติ | Enter ส่งข้อความ (ไม่ขัด Shift+Enter) | ปิดแผงทั้งหมด | ✅ |
| ปุ่มรับ/ปฏิเสธ/วางสาย (วิดเจ็ตใหม่ + admin) | ปกติ | ทำงานตามคาด | ไม่ปิดสายด้วย Escape (ตั้งใจ ป้องกันวางสายพลาด) | ✅ ออกแบบเหมาะสม |
| `DialogProvider` (confirm/prompt/alert) | เข้า dialog ได้ แต่ **ไม่ trap โฟกัส** | ทำงานตามคาด | ปิด/ยกเลิก | ❌ ดูข้อ 7 |
| เมนูนำทางหลัก | ไม่มี skip link | ปกติ | — | ❌ ดูข้อ 6 |

## Screen Reader

| Element | Announced As | Issue |
|---------|-------------|-------|
| `.sw-panel` (แผงแชทใหม่) | "dialog, แชทกับทีมงาน" | ไม่มี — ใช้ `aria-label` ตรง |
| สถานะสาย (`role="status"`/`role="alert"`) | ประกาศอัตโนมัติเมื่อเปลี่ยนสถานะ เช่น "กำลังขอให้พนักงานโทรกลับ", "พนักงานกำลังโทรเข้า" | ไม่มี — ใช้ live region ถูกต้อง |
| ปุ่มไอคอนในวิดเจ็ตใหม่ (รับสาย/วางสาย/ปิดไมค์ ฯลฯ) | ชื่อปุ่มตาม `aria-label` ที่ตั้งไว้ (เช่น "รับสาย", "วางสาย", "ปิดไมค์") | ไม่มี — ครบทุกปุ่ม |
| `.dlg-card` (dialog ระบบเดิม) | "dialog" เฉยๆ ไม่อ่านหัวข้อ/ข้อความต่อ | ขาด `aria-labelledby`/`aria-describedby` — ดูข้อ 10 |
| `.dlg-toast` | ประกาศข้อความผ่าน `aria-live="polite"` | หายเร็วเกินไปสำหรับผู้ที่ใช้เวลาฟัง/อ่านนาน — ดูข้อ 9 |

## Priority Fixes
1. **แก้คอนทราสต์สี `--muted` และปุ่มเขียว/แดงตัวอักษรขาว (ข้อ 1–2)** — กระทบข้อความรองและปุ่มหลักนับร้อยจุดทั่วไซต์ ผู้ใช้สายตาเลือนรางอ่านลำบากที่สุด
2. **เพิ่ม focus trap + label ผูก ARIA ให้ `DialogProvider` (ข้อ 7, 10)** — ใช้ทั่วไซต์ทุกครั้งที่ confirm/alert/prompt ผู้ใช้คีย์บอร์ด/screen reader หลุดออกจาก dialog ได้ง่าย
3. **เพิ่ม skip link (ข้อ 6)** — ปรับปรุงประสบการณ์ผู้ใช้คีย์บอร์ดในทุกหน้าด้วยการแก้ที่เดียว (`layout.tsx`)
4. **ขยายปุ่มไอคอนเล็ก + toast ปรับเวลา (ข้อ 8–9)** — ผลกระทบจำกัดกว่า ทำเป็นรอบถัดไปได้

---

*ฟีเจอร์ "ติดต่อพนักงาน" ที่สร้างใหม่ (`SupportWidget.tsx`, `admin/support/page.tsx`) ผ่านเกณฑ์ WCAG 2.1 AA ในทุกหัวข้อที่ตรวจ ยกเว้น 2 จุดที่สืบทอดโทนสีเดิมของไซต์ (ข้อ 3, 8) — ไม่ใช่ข้อบกพร่องที่ออกแบบใหม่*
